function timeslash() {
/*
	//step
	if (currentSkill == "Time Slash") {
		if (skillTimer == 100)
			global.pause = true;
		part_particles_create(global.particleSystem, x, y, global.particleShadow, 1);
		x+=2;
		if (skillTimer == 1)
			global.pause = false;
		skillTimer--;
	}
	
	//draw
	if (currentSkill == "Time Slash") {
		draw_set_color(c_black);
		draw_set_alpha((100-skillTimer)/100);
		draw_rectangle(0, 0, room_width, room_height, false);
		draw_set_alpha(1);
	}
*/
}
event_inherited();
if (variable_instance_exists(id, "target")) {
			if (instance_exists(target) && instance_exists(source) && source.currentSkill == skill && counter == image_number*2) {
					stun(target, 30);
			}
}